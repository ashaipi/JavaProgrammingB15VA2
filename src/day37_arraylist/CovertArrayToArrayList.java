package day37_arraylist;
import java.util.*;

public class CovertArrayToArrayList {

    // HOW TO CONVERT ARRAY TO ARRAYLIST?
    // I can use Arrays.asList method.
    // List<Integer> list = Arrays.asList(array);

    //HOW TO CONVERT ARRAYLIST TO ARRAY?
    // I can use toArray method of arraylist
    //Object[] arr1 = list.toArray();

    //list.toArray(); //is used to convert from ArrayList to an array.

    //But it returns Object[] array, not a Integer.
    //To get integer array, we need to add more code.
    //Integer[] arr2 = list.toArray(new Integer[0]);
}
